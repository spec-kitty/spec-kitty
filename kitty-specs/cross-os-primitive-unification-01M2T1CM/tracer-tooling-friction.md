# Tracer: Tooling Friction — Cross-OS Primitive Unification (01M2T1CM)

Seeded at planning; append during implementation. Friction observed while
driving this mission feeds the next mission's tooling improvements.

## Planning

- (2026-09-18) `spec-kitty mission list --json` / `charter context` emit a
  `LegacyOrgPackDoctrineKeyWarning` for `.kittify/config.yaml`'s legacy
  `doctrine.org.packs` key — noise, not blocking; noted for a config-canonicalization pass.

## Implementation

_(append as encountered)_
