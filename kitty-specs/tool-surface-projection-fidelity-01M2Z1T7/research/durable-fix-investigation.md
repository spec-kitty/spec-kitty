# DIRECTIVE_052 (Prefer Durable Fixes) — Investigation Record

**Mission:** tool-surface-projection-fidelity-01M2Z1T7
**Base:** grounded against `upstream/main` @ `a2952f33ac`; both defects reproduced (installed CLI 4.0.0rc4, matching the QA reports).

## 1. Tracker search (whole tracker) + umbrella

No existing umbrella epic for "tool-surface projection honesty" — **propose one** and parent the class under it.

- **In scope (confirmed by operator):** #4782 (P0, orientation no-op), #4776 (P0, upgrade needs 3 runs), #4777 (P3, Windows dry-run phantom repairs), #4134 (upgrade `_recheck_command_completion` `installed_at` race).
- **Deferred under the umbrella:** #2527 (P2 — doctrine-skill manifest-hash drift + stray `dist/`, adjacent theme different surface), orientation-refresh split-brain (DIRECTIVE_044 structural).
- **Closed context (do not reopen):** #4703 (Windows self-held lock), #4706/#4707 (orientation content bugs — `project: unknown`, health never `upgrade-available`). Evidence the session-presence surface has a recurring honesty-defect cluster.
- **Out of scope:** #3985/#3986 (lane-local migration-test debt from PR #3923, not on main).

## 2. Code grounding (file:line, verified live on upstream/main)

**#4782 — session-presence repair applicability.**
- Detect (works, no gate): `tool_surface/providers/session_presence.py::probe` → `_orientation_version_is_stale` (:690-707) → `_on_disk_orientation_version` (:651-681). Pure version-string mismatch → `STATE_STALE`.
- Repair (no-op): `repair` (:481) → `assess` → `_prepare` (:228-238) sets `applicable = writer.can_write(root)`. Gemini writer `MarkdownRulesWriter("gemini", "GEMINI.md", append_mode=True, check_dir=".gemini")` (`session_presence/writers/registry.py:42`); `can_write` (`writers/markdown_rules.py:134-138`) returns `(project_root/".gemini").exists()`. `GEMINI.md` is at repo **root** → when `.gemini/` absent, gemini dispositioned `not_applicable`, returned under `skipped` (not `failed`) → doctor exits 0 unchanged.
- **Empirically reproduced** (both branches): `.gemini/` present → repaired; absent → stale detected, repaired=(), skipped=(gemini), file unchanged, no failure.
- Latent twin: `llxprt` (`LLXPRT.md`, `check_dir=".llxprt"`) — identical shape. `AgentsMdWriter.can_write` (`writers/agents_md.py:37-39`) is always-True at root — the correct model for a root-level file.
- **No regen-assets exposure**: orientation is runtime-generated by `session_presence/content.py::SessionPresenceContent.render()` (:48-49), NOT a `packs/` SOURCE (`git grep "spec-kitty:orientation" packs/` = 0). Format-drift ruled out (the real 3.2.6 block canonicalizes fine via `_canonical_section`).

**#4776/#4777 — managed-skills completion re-check (Windows POSIX-mode).**
- `tool_surface/providers/managed_skills.py:165-200` `_recheck_command_completion`; raise at :177-180 (`Completed command output changed: …`). For a directory, `observe_skill_path().state` has `mtime_ns=None` (`skills/paths.py:79-81`); mtime normalized out at :179 → comparison reduces to **`mode`**. Shared-parent effect pinned to `mode=0o755` (`managed_skills.py:127-132`); dirs created `mkdir(mode=0o755); chmod(0o755)` (`skills/command_installer.py:900-901`). On **Windows `os.chmod` can't represent POSIX mode** → observed mode ≠ 0o755 → raise → `apply_composition` (:345-349) returns before `apply_installation` (:351) → doctrine skills never apply on run 1. The 15→70→70 progression: command skills apply before the recheck abort; on run 2 `.agents/skills` exists (no longer an `absent→create` effect) → recheck passes → doctrine applies. Abort string surfaced at `cli/commands/upgrade.py:514`.
- **#4777 is the SAME root cause** (code agent): its 184 phantom repairs are exactly the plan's `chmod` effects, re-planned forever because `os.chmod` is a Windows no-op. One Windows-aware mode fix closes both.

**#4134 — ride-along, same function, different field.** `installed_at` wall-clock in `command-skills-manifest.json` (`skills/command_installer.py:610`, `skills/manifest.py:120` retains prior `installed_at` on same-identity re-write but not first-write/dir-mtime churn) → a **file** effect sha256 mismatch (a race), distinct from the **directory** mode cause. Folded because it rides the same `_recheck_command_completion` seam; its own fix (deterministic manifest content) does not close #4776 and vice versa.

## 3. Architecture alignment (canonical seam → DIRECTIVE_044)

- Shared driver: `SurfaceRepairService` via `upgrade/assessment.py:200-214` (both `upgrade` and `doctor --fix`). Providers self-register via `providers/_discovery.py` into `SurfaceProviderRegistry`.
- **Two seams, one class.** Canonical authorities (`WRITER_REGISTRY`, `SkillRegistry`, `SurfaceProviderRegistry`) are already single/shared — the defects are (a) a repair-side predicate diverging from the detect-side predicate, and (b) non-deterministic / host-unrepresentable generated state at the completion re-check. Not a single unification fix.
- DIRECTIVE_044 **does** apply to the secondary orientation-refresh split-brain (≥3 refresh implementations: `SessionPresenceManager`, `RefreshOrientationBlockMigration` `m_3_2_0rc39`, `SessionPresenceProvider.repair`) — deferred as a structural follow-up (C-004), not the P0 fix.

## 4. ADRs

- `docs/adr/3.x/2026-06-07-2-session-presence-multi-harness-architecture.md` — four pattern-writers; **"we do not silently skip a supported harness"**. The gemini `not_applicable` skip violates the spirit of this ADR; the fix restores it. No amendment.
- `docs/adr/3.x/2026-07-23-1-surface-vocabulary-two-domains-and-topology-surface-rename.md` — surface vocabulary. Aligned.
- `docs/adr/3.x/2026-07-19-2-skill-projection-copies-not-symlinks.md`, `2026-04-08-3-global-skill-installation-per-project-symlinks` — skill projection. Aligned; no amendment.
- MEMORY `os-replace-readonly-posix-vacuity` — the POSIX-mode-on-Windows pattern; tests simulate Windows by monkeypatching observed mode.

## 5. Structural remediation & fold-vs-defer (surfaced before spec)

Operator chose **Core + Windows + #4134 ride-along**: #4782 (+llxprt) + #4776 + #4777 + #4134. Deferred #2527 + orientation-refresh split-brain under a new umbrella epic. Fix generating code, never generated files (C-001). Repair must converge in one run or report failure — never silent-skip/exit-0 (the honesty standing order, FR-009).

**Compliance:** root causes diagnosed to two systemic seams; the Windows class closed by folding #4777; #4134 folded on the same function; the larger structural items (scanner-adjacent split-brain, #2527 different surface) recorded behind an umbrella — not silently dropped, not over-engineered onto the P0 slice.
