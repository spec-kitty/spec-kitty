# Tooling Friction Log

> Log every place the tooling fought you so it can feed the tooling-gap backlog.

**Prompting questions**
- What tooling or command did you have to work around?
- What blocked you unexpectedly, and how long did it take to unblock?
- Was this a known issue or something discovered fresh?

---

## Entries

<!-- YYYY-MM-DD — 1-3 sentences: what happened, why it slowed you down. -->

- 2026-09-16 — The handover carried stale coordination metadata; the orchestrator repaired it via canonical doctor coordination --fix. Source inspection found the plan’s stale consistency_check path and four human-only helper callers; task ownership uses actual paths and preserves those callers’ existing interface.

- 2026-09-16 — The requested monolithic tasks prompt directs manual tasks.md authoring, while the active staged tasks-outline prompt requires wps.yaml and generated tasks.md. Added the canonical manifest from identical WP metadata and reran finalization; no dependency or implementation scope changed.

- 2026-09-16 — Concurrent CLI bootstrap calls raced on the global slash-command asset assessment (`Global asset input changed`). Workflow commands are now serialized through a temporary workspace lock; source implementation remains parallel. No SaaS-sync setting was changed.

- 2026-09-16 — A fresh compact-identity implementation claim created the lane, then rejected itself as already claimed (upstream https://github.com/spec-kitty/spec-kitty/issues/4665). Documented agentless resume rendered the prompt without ownership override. Explicit model/profile flags require a correlated durable dispatch invocation; they cannot substitute self-asserted provenance. Claims retain truthful profile metadata and use the supported resume path when needed.

- 2026-09-16 — Generated prompt paths disappeared between production and consumption while baseline gates and workflow calls were running. Immediate copies of the official generated bytes into the isolated workspace parent preserve the prompt for agent consumption; Root pytest teardown deletes any newly created prompt in its checkout namespace, including live CLI prompts; tracked separately in https://github.com/spec-kitty/spec-kitty/issues/4666.

- 2026-09-16 — Concurrent broad tests caused baseline E2E timeouts in dependent-WP approval (900s) and uninitialized-specify fixture setup (120s), with no failed contract assertion. Both nodes passed unchanged under reduced load (2 passed, 549.16s); the other three scenarios passed initially. Preserve the original failures and rerun evidence rather than treating timeouts as waivers. Tracking: https://github.com/spec-kitty/EXPERIMENTAL-spec-kitty-end-to-end-testing/issues/472. Final integrated E2E verification remains required.

- 2026-09-16 — The full CLI test directory includes a 269-leaf help traversal. A bounded unchanged-source diagnostic completed 18 leaves at a median 9.4885s each, predicting roughly 42.5 minutes for that test alone. This explains long CPU-bound execution independently of the JSON seam changes; the original subsystem run remains authoritative.

- 2026-09-16 — Located the existing exact help-test performance issue #4636 and parsing issue #4516. A single-call profile attributed 78% to global agent command preparation (104 renders, 416 glossary seed loads), 20% to skill assets, under 0.5ms to project-root discovery, and no HTTP/socket calls. Stop the known unrelated traversal, retain its interrupted status and completed tests, and diagnose the four actual subsystem failures; do not report the interrupted case as passed or expand this mission into caching work.

- 2026-09-16 — WP02's interrupted broad CLI run retained 4,170 passes, 10 skips, two expected failures and four real failures. All four failed identically on unchanged baseline source in 6.29s: stale completion manifest (#4479), two org-cascade negative cases (#4668), and the decision-command shape expectation (#4669). The separate full-corpus subprocess timeout passed unchanged under reduced load (347.63s). These baseline findings stay outside the JSON boundary implementation and remain explicit in review evidence.

- 2026-09-16 — Fix-mode claims for WP03 and WP05 printed successful implementer claims but retained the previous reviewer in reduced ownership, blocking ordinary for_review submission (#4673). After verifying each claim and actual implementer, the supported same-lane in_progress reassignment repaired ownership with an explicit reason. No approval, reviewer finding, or pre-review gate was bypassed; ordinary submission and independent review remain required.

- 2026-09-16 — WP05's distinct subsystem run passed 1,553 tests but failed 33 cases. The exact same failures reproduced against unchanged original source: 32 tests indexed retired research/plan YAML keys (#4671), and one real local relay test rejected DecisionPointOpened and received no SSE frame (#4672). The original and baseline logs remain available; neither failure class is repaired or hidden by this mission.
