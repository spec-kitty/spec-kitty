# Tracer: Design Decisions & Rationale

Append one entry per non-obvious decision made during implement. Seeded at planning.

- 2026-09-20 (plan): #4758 fix = co-locate lanes.json write in legacy finalize + gate move-task from leaving planned (defense in depth). Rationale: close the class by construction, not only cure it.
- 2026-09-20 (plan): recovery extends `agent mission repair` (not a 4th authority); reuses extracted `_compute_and_write_lanes` + `doctor.py:215` detector. Rationale: DIRECTIVE_044 single canonical authority.
- 2026-09-20 (plan): #4786 taken as full structural fix per operator ruling despite being clearable today; recovery/merge-guard are defense-in-depth. Recorded honestly in spec Assumptions.

- 2026-09-20 (post-plan, debugger-debbie): #4786 root cause = `agent` is a conflated live-claim/current-actor slot (changes hands implementer→reviewer); releasing on rollback is CORRECT (keep #4673 byte-for-byte). Real miss: nothing owns durable implementer PROVENANCE. FIX RELOCATED: FR-005 moves from tasks_move_task.py write path → a read-root projection `_project_implementer_attribution` in status/reducer.py (mirror `_project_cancellation_provenance` reducer.py:67-90); accept gate summary_core.py:79/87 reads the derived slot. Do NOT touch tasks_move_task.py:2891/:2970 or upstream spec_kitty_events.diary fold. Honest by construction (never-owned → no claim event → no derivation → gate correctly refuses). Consequence: WP03/FR-004 shrinks to pure lanes.json rebuilder (drop attribution-restore); doctor.py:214-249 narrows to genuine on-disk blanks. Write-scope shifts: attribution WP now owns status/reducer.py + acceptance/summary_core.py (+ doctor.py), NOT tasks_move_task.py.

- 2026-09-20 (post-plan, architect-alphonso): recovery HOME changed agent mission repair → `doctor mission-state --fix` (migration/mission_state.py:685 repair_repo, already --mission-scoped + primary-anchored, un-gated, co-located with the status/doctor detector). Reason: agent mission repair early-returns on `shas is None` (mission_repair.py:286-292) = exactly the non-coord topologies where the #4758 wedge lives. Operator scope ruling (recovery command exists) preserved; only the home corrected on evidence.
- 2026-09-20 (post-plan, architect-alphonso): _compute_and_write_lanes extraction = pure-core to new lanes/compute_and_persist.py (takes planning_commit_sha + mission_id as resolved inputs), CLI wrapper stays in mission_finalize.py. Verbatim move would invert layers (console/JSON/policy into lanes/). One production caller (mission_finalize.py:2829).
- 2026-09-20 (post-plan, architect-alphonso): BROAD execution-ready predicate unification REJECTED/DEFERRED (unsafe: file-existence gates need the LanesManifest object; file-vs-log disagreement IS the #4758 signal; ~6 soft sites legitimately None-tolerant). Only the narrow 2-caller wedge predicate (execution-begun AND lanes-absent) is folded in-mission.

- 2026-09-20 (post-tasks, reviewer-renata anti-laziness, folded): (1) added docs/changelog/CHANGELOG.md to WP04 owned_files (T021 was a hidden cross-scope write); (2) T017 now rewords the never-owned accept "missing agent" refusal to name the repair (FR-006/SC-003); (3) WP03 T012 adds corrupt-log fail-closed (NFR-003); (4) WP03 T013 tightens NFR-002 to an enumerated parity table; (5) WP04 T022 runs test_no_legacy_terminology (C-004). Spec FR-004 + US1 scenario reworded — attribution is DERIVED (WP04), not restored by the recovery command. Red-first/stub/terminology checks PASSED.

- 2026-09-20 (implement env): two env blockers hit before WP01 impl. (1) stale global cache ~/.kittify/cache/slash_commands-assets.json "input changed" (the #4776 convergence-recheck class) → removed the stale cache (2 prior .bak files show it recurs). (2) legacy sparse-checkout scan aborts on OTHER missions' worktrees (census-floor, cross-os-primitive) — primary is core.sparseCheckout=false, so used --allow-sparse-checkout (surgical; did NOT run doctor --fix which would touch the operator's other in-flight worktrees, and is a no-op non-interactively anyway).
