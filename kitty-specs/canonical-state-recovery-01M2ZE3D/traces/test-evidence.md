# Tracer: Test Evidence (red-first proof + blast radius)

Append the exact command + passed/failed counts per WP. Seeded at planning.

- Pending WP01: #4758 wedge repro RED through `agent tasks finalize-tasks` + `move-task --to doing` before fix.
- Pending WP02: #4786 rejection-cycle repro RED through `accept` before fix.
