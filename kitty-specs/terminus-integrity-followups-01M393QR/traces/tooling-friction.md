# Tooling-Friction Tracer — terminus-integrity-followups

Append friction hit during the mission (per SO#3). The prior mission's tracer flagged the terminus/lane machinery fighting the operator — this mission FIXES that class, so friction here is doubly informative.

- (seed) CLI on PATH runs from a sibling checkout without the #5012 spine; mitigation: drive everything through .venv/bin/spec-kitty and .venv/bin/python -m pytest.
