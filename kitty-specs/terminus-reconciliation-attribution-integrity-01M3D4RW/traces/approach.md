# Tracer: approach

One entry per finding: `YYYY-MM-DD · actor · <text>`.

---

2026-09-25 · claude · Severity-ordered WPs: WP1 #5022 (data-loss squash deletion attribution — foundation, adds ApprovedWpCommitSet.authored_deletions + _final_authored_deletions), WP2 #5018 (commit-level exclusion, reuses WP1's authorship threading), WP3 #5021-r1 (resume tolerates completed-but-mid-teardown squash), WP4 #5038 (bookkeeping-projection proof precision). #5021-r2 3-way kept honest xfail. Two opus grounding lenses confirmed all four LIVE on HEAD 1409dc827f; no supersession.
