# Issue Matrix — Coordination Doctor Branch Safety

| Issue | Title | Verdict | Evidence ref |
|-------|-------|---------|--------------|
| #4920 | `doctor coordination --fix` mutates the wrong checked-out branch | in-mission | WP01 will add the red-first real-Git reproduction, fail-closed repair guard, postcondition check, and validation evidence. |

Valid `Verdict` values: `fixed`, `verified-already-fixed`,
`deferred-with-followup`, `in-mission`. No row may remain `in-mission` at close.
