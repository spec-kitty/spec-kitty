# Tooling friction

- Research starts the runtime override workflow but research CLI requires a plan. Research/setup-plan/context lack explicit owned-checkout flags. Use the canonical template resolver and supported spec-commit; never fabricate runtime state.
- Intake described older commit and charter behavior; fresh source audit prevented duplicate fixes.
