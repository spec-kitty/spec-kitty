# Tracer: design-decisions

One entry per finding: `YYYY-MM-DD · actor · <text>`.

---

2026-09-26 · claude-orchestrator · DM host_tactic: new dedicated tactic acceptance-criteria-non-vacuity (operator; squad preferred extending acceptance-test-first). DM overrides: refresh .kittify/overrides too + file broader drift gap. Label shape: trailing FR-table columns 'Delivery' / 'No-op passable?' with bracketed values; SC gets a trailing suffix; no inline/ID-cell labels (C-004). Spec dogfoods the columns; parser declares all 10 FRs.

2026-09-26 · wp03-implementer · WP03 T013 override-only-line inventory (pre-resync counts via 'diff override builtin | grep -c ^<'): actions/implement/guidelines.md=2 (line3 stale 'constitution'->built-in 'charter' terminology; line11 newer override wording ported upstream via T013a before resync), actions/review/guidelines.md=3 (line3 stale 'constitution'; lines9,21 newer override wording ported upstream via T013a), actions/plan/guidelines.md=5 stale, actions/{implement,plan,review}/index.yaml=1 each (override missing many newer directives/tactics/procedures accumulated in built-in since drift began — no local-only content, pure staleness), actions/specify/guidelines.md=1 stale, mission.yaml=1 (stale 'constitution' wording), mission-runtime.yaml=26 (override still on v2.1.0 tasks_* 3-step DAG; built-in on v2.2.0 single 'tasks' step + DEPRECATED-marker header noting StepContractExecutor now owns dispatch — squad-accepted live risk, not data loss), templates/plan-template.md=27, templates/spec-template.md=9, templates/task-prompt-template.md=1, templates/tasks-template.md=23 (all stale legacy template text superseded by built-in), command-templates/{accept,analyze,implement,plan,review,specify,tasks,tasks-finalize,tasks-outline,tasks-packages}.md=52/22/149/9/96/63/25/14/1/4 respectively (override prompts pre-date multiple built-in revisions: old 'Constitution Authority' language, old CLI flag/JSON-field names like mission_dir vs feature_dir and check-prerequisites without --mission, missing Issue-Matrix Approval Heads-Up section, missing record-analysis persistence step). No local-only intentional doctrine was found in any drifted file — every override-only line is either stale pre-rename text or the two T013a-identified newer lines (now ported upstream first). Same-content (0 override-only lines): README.md, actions/specify/index.yaml, templates/README.md. expected-artifacts.yaml deleted per DM-01M3EXVSGFRQ8YKVFCCBWVSVZD (inert mirror, not resynced). No follow-up upstream-porting candidates identified beyond the two already handled in T013a.
