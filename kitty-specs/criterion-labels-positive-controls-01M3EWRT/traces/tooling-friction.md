# Tracer: tooling-friction

One entry per finding: `YYYY-MM-DD · actor · <text>`.

---

2026-09-26 · claude-orchestrator · specify: on a fresh coord-topology mission, 'agent decision open' raises CoordinationWorktreeUnmaterialized (read-before-write in decisions/service.py _mission_dir -> read_dir(STATUS_STATE)); the error's advertised recovery 'spec-kitty doctor workspaces --fix' only removes husks and does not materialize. The 'self-materializes on first coordination-branch write' promise is unreachable for decision open / tracer-append (both read first). Worked around by calling the canonical CoordinationWorkspace.resolve(). Also: the failed open still persisted the ledger entry (later open reported idempotent:true). To be filed upstream.

2026-09-26 · claude-orchestrator · specify: spec-commit of decisions/{index.json,DM-*.md} routed to the COORD branch (61f625a) while decisions/service.py::_ledger_dir documents the ledger as PRIMARY-partition (#4966 AC-D2); status.events.jsonl (decision events) stayed modified in the primary checkout and was silently not committed. Split-brain residue left in primary.

2026-09-26 · claude-orchestrator · tasks: finalize-tasks committed tasks to primary and WP status transitions to the coord branch, but left mission lifecycle events (TasksStarted/WPCreated/TasksCompleted) in the primary status.events.jsonl + an untracked status.json, uncommitted. Also: 'agent context resolve --action tasks' returns feature_dir = coord worktree while check-prerequisites returns the primary dir. And map-requirements rewrote WP frontmatter unquoted, so a sed on the quoted agent_profile missed. finalize --validate-only correctly refused a non-activated profile (doctrine-daphne) -> switched WP01 to curator-carla.

2026-09-26 · wp02-implementer · test_requirement_mapping.py already failed 'ruff format --check' before this WP touched it (pre-existing formatting debt, confirmed against HEAD~2); reformatting the whole WP-owned file was required to pass the NFR-004 gate on the changed file, producing a larger-than-minimal diff. Also: is_substantive()'s real signature is (file_path: Path, kind), not (text, kind) as the WP prompt's T008 example implied -- verified against source (line 865) before writing tests, per the prompt's own 'check the exact signature' caveat.

2026-09-26 · wp01-implementer · T002/T006: the WP prompt's suggested tactic references list (architectural-gate-non-vacuity, delete-the-assertion-not-the-test, acceptance-test-first) is unsafe as written. Combined with the FR-008-mandated forward edges from acceptance-test-first/atdd-adversarial-acceptance, referencing delete-the-assertion-not-the-test or acceptance-test-first back closes a 2-3 node cycle (tests/doctrine/test_directive_consistency.py::test_tactic_reference_graph_has_no_cycles). Separately, referencing architectural-gate-non-vacuity bridges into directive:DIRECTIVE_043's densely-connected region (adversarial-squad-cadence -> brownfield-onboarding -> DIRECTIVE_041/034), moving every governance-bearing mission type's charter activate --cascade all total by dozens of unrelated artifacts (documentation: 40->122) -- caught only by running the full tests/doctrine tests/charter suite and diffing against the mission base commit in a scratch worktree, not by the WP's own Test Strategy commands. Recommend: any future tactic that references an already-widely-reachable sibling should get a cascade-total smoke check as part of its own acceptance test, and the mission-step guidance for authoring new tactic 'references' lists should warn about hub nodes (DIRECTIVE_043 in particular).

2026-09-26 · claude-orchestrator · implement WP03: (1) implement gate refused with analysis_report_required after spec/plan were re-committed post-analysis; error text says 'must be run inside your coding agent' rather than 'stale report' — misleading. (2) lane-c allocation failed: merging recorded planning_commit_sha a09b2612 into the lane conflicted add/add on decisions/index.json (ledger split-brain: spec-commit routes it to coord, decisions service writes primary). finalize-tasks re-run does NOT refresh planning_commit_sha. Recovered per the CLI's own instruction (manual merge into a lane-c worktree, resolved to the coord superset, re-ran implement). (3) the event-log merge driver 'spec-kitty merge-driver-event-log' fails with 'spec-kitty: not found' when the venv is not on PATH of the git process. (4) move-task after a rejection requires --force and records an 'Arbiter Override' for a plain resubmission.
