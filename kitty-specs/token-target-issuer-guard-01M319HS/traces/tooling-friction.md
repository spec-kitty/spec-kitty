# Tracer: Tooling Friction

Append friction encountered with tooling/CLI/tests during this mission. Seeded at planning.

- 2026-09-21 (planning): `spec-kitty` global install is an editable install of this repo at 4.0.0rc4;
  `upgrade --agent-check` reports latest as `3.2.7` from PyPI (pre-release 4.x not on PyPI yet) — the
  "latest_version < installed" is expected for an rc dev checkout, action `none`. No friction, noted so
  a later agent does not chase a phantom downgrade.
- (append during implement)
