# Tracer: Tooling Friction

Seeded at planning; append during implementation. Friction feeds the next mission.

## Observations

- `spec-kitty charter context --action specify` emits `CharterCatalogMissWarning` noise for
  java/maven/typescript artifacts scope-filtered out under the python language set. Cosmetic, but
  clutters agent output. (pre-existing; not this mission's scope)
- No `spec-kitty agent mission issue-matrix` CLI surface — issue-matrix.json is authored by hand.

## Notes (append during implement)
-
