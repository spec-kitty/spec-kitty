# Contracts

This mission introduces no new API / wire contract. The one behavioral contract it changes — the
squash projection proof (byte-equality → driver-replay attribution) — is specified in
[`../data-model.md`](../data-model.md) ("Proof contract" + the four fail-closed invariants
INV-FLOOR-1/2/3 and INV-NO-REGRESSION).
