# Tracer: Tooling Friction — Finalize re-pins an orphaned planning_commit_sha (#4827)

Seed at planning; append during implement.

- **Editable install cannot rebuild here**: the corporate artifactory index lacks `hatchling`, so `pip install -e .` fails at build-dep resolution. The venv is already editable against this repo's `src/specify_cli` (v4.0.0rc4), so source changes are live — but any change to CLI entry-point metadata (unlikely for this mission) could not be re-registered via reinstall. No entry-point changes are planned.
- **Blast radius outside `make test-fast`**: the regression lens flagged that the corrected-expectation and new tests live OUTSIDE `make test-fast`'s dirs (`tests/specify_cli/cli/commands/agent/`, `tests/lanes/`). The PR's *Tests run* must explicitly include those dirs or CI catches breaks a narrow local run misses. Golden-contract + doc-freshness gates are the specific CI-only reds to watch.
- **Doc-freshness gate**: adding `--allow-orphaned` changes the rendered finalize-tasks help body, so `docs/api/agent-subcommands.md` must be regenerated via `scripts/docs/build_cli_reference.py` or `scripts/docs/check_cli_reference_freshness.py --ci` reds. (command-skills-manifest does NOT enumerate the flag → no manifest regen.)

## Implement log
- **2026-09-21 — dogfooded L1/L2 live (the very friction this mission fixes).** Drove WP01 through the lane-worktree flow; `move-task WP01 --to for_review` was refused by the lane-branch hygiene gate ("kitty-specs/ changes are not allowed on lane branches") because the `spec-kitty implement` bootstrap auto-commit (`d2fdcafa37 "chore: Start WP01 implementation"`) wrote `tasks/WP01-pin-classifier.md` + `status.*` onto the lane branch. Compounded by the coord/primary partition split: planning artifacts (spec/plan/tasks/research/tracers) live on the `fix/` primary partition while lifecycle (status/issue-matrix/acceptance) routes to the `kitty/mission-…` coord branch, and the lane branched from coord — so the lane's `kitty-specs/` tree diverges from both. This is exactly field-report **L1** (lane-worktree ↔ mid-mission friction) and the reason **L2** exists.
- **Resolution (field-report L2, single-orchestrator sequential):** pivoted to implement-on-mission-branch. WP01's code commit (`3fc9380724`, src+tests only, 17 tests green, ruff/mypy clean) was cherry-picked (`-x`) onto `fix/finalize-repin-orphaned-planning-commit` (→ `61d430bdd9`) and the lane-a worktree was torn down. WP02–WP04 implement directly on the mission branch for a clean linear history; per-WP opus review discipline continues. The final PR targets upstream `main` from `fix/…`.
