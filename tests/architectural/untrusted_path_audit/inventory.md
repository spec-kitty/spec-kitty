# Untrusted-segment → FS-sink inventory (WP01 / FR-003, FR-004)

<!-- markdownlint-disable MD037 -->

Generated input: `python tests/architectural/untrusted_path_audit/audit.py`
walks `src/specify_cli` and self-asserts this table in BOTH directions —
undercount (every discovered sink is documented) AND overcount/ghost (every
documented row still maps to a live sink), plus known-candidate presence and the
FR-009 `meta.json` row. Dispositions are human-verified against the cited
source — see `RULESET.md` for the vocabulary and the Named-untrusted rule.

**Row identity (FR-004).** Rows are compared by the drift-proof composite
`(file, qualname, token)` — the `qualname` + `token` columns, derived by
`tests.architectural._ratchet_keys.composite_key_from_file`. The **`line` in the
`file:line` locator is NON-authoritative** (a jump-to aid, never compared): a
blank/comment insertion that shifts a sink's line leaves the audit GREEN (this
killed the #2306 false-failure class). A real edit to the sink line changes the
`token` and the audit goes RED. Long tokens are truncated at 60 chars with a `…`
marker (the stored prefix stays unique per `(file, qualname)`).

**Freshen path.** When a sink's content genuinely changes, run the audit: the
undercount error prints the exact `| file:line | qualname | token | … |` row to
paste. Never hand-type a token — it must be tool-derived (`composite_key_from_file`).

**`[inventory-only]` rows.** A row tagged `[inventory-only]` in its rationale is
exempt from the overcount guard because the AST matcher intentionally cannot
discover it — a RULESET known-false-negative class (the FR-009 `meta.json`
write-path keyed on `feature_dir`, or a cross-function diagnostic whose join sits
behind a boundary seam). Each tagged row names WHY. (No *removed-sink* tags exist
at conversion; the 5 tags are all known-FN documentation.)

**Note (WP02/WP03 update):** WP02 added `_is_contained` to `status/store.py`
(new `mission_slug` join; old join shifted out of scope) and WP03 added seam
calls to `dossier/`, `events/`, `migration/`, and `review/arbiter` (those joins
are no longer discovered).

**Conversion record (FR-004 re-key, WP02, recorded).** Every `qualname`+`token`
below was tool-derived by `composite_key_from_file` — reproduce any of them by
re-running that helper on the row's `file:line` (or delete a row and re-run the
audit: the undercount message prints the exact replacement). Discovered rows
froze their live `(qualname, token)`; five rows the matcher cannot AST-discover
(RULESET known-FN) were refreshed to a real current line, tagged
`[inventory-only]`, and exempted from overcount: `mission_metadata.py:458`
(FR-009 write-path), `cli/commands/decision.py:552` (cross-fn `load_meta`),
`status/aggregate.py:514` (`_find_meta_path`), `status/aggregate.py:762` and
`:763` (`save()` diagnostics). The stale duplicate `review/cycle.py:225` (a
second copy of the live `:231` legacy-feedback join) was dropped — the overcount
guard's exact reconciliation purpose.

## Sink table

| file:line | qualname | token | untrusted source | sink op | disposition | rationale |
| --- | --- | --- | --- | --- | --- | --- |
| audit/engine.py:88 | _resolve_mission_filter | candidate = scan_root / resolved . mission_slug | resolved.mission_slug | Path-join (/) | unreachable | `scan_root / resolved.mission_slug` is consumed only by a `.is_dir()` existence filter that returns an empty `frozenset` on miss — no file is opened/written; a traversal slug simply fails `.is_dir()`. (Named-untrusted rule: a value that IS `mission_slug` is dispositioned `unreachable`, not `trusted-source`, even though `resolve_mission` resolves it against the on-disk index.) |
| cli/commands/agent/tasks_move_task.py:296 | _mt_warn_worktree_kitty_specs | if ( worktree_kitty / st . mission_slug / ) . exists ( ) : | st.mission_slug | Path-join (/) | routed-through-seam | #2037 closure: `st.mission_slug` is now checked with `assert_safe_path_segment` before the `worktree_kitty / st.mission_slug / "tasks"` `.exists()` probe; an unsafe slug logs a warning and skips the informational note (fail-closed — this probe only gates a console print, so skipping it has no functional impact). (line shifted 206→296 by this fix: the guard clause + early `if worktree_kitty is None: return` restructuring moved the `.exists()` check onto its own line below the new validation block; same sink, disposition now closed. Prior history: shifted −11 by mission 01KVJPEQ read-side adoption rebase; then +2 by mission 01KVMBD6 #2058 god-tag comment; then 1902→1933 by mission single-planning-surface-authority-01KVPR00 WP06 map-requirements read-surface consolidation; then 1933→1932 by WP03 import shrink; then 1932→1935 by mission single-authority-topology-cleanup-01KVRJ6P WP16 commit-target enum eradication, FR-001b; then 1935→1972 by mission write-surface-coherence-01KVTVZS kind-aware read-path resolver additions; then 1972→1046 by mission decompose-agent-tasks-god-module-01KVWVAR #2058 seam extraction (helper bodies moved into tasks_*.py modules, shrinking tasks.py); then 1046→1079 by mission single-authority-resolution-gates-01KW1P0F WP02 (#2154 write-leg routing + #2155 mixed-bundle partition + T012 canon-site folds added lines above this sink); then 1079→1076 by mission implement-loop-coord-authority-completion-01KW2E7A (#2160) routing planning reads onto resolve_planning_read_dir (net −3 lines above this sink); then 1076→1077 by the 2026-06-27 rebase onto upstream/main carrying concurrent mission #1057's check_pre30_layout boundary-guard insertions (+1 line above this sink); then 1077→1134 by PR #2277 reliability-papercut-sweep 7-WP coexistence (+57 lines above this sink); then 1134→1325 by mission tasks-py-degod-01KWF08S (#2116) which extracted this probe VERBATIM into the `_mt_warn_worktree_kitty_specs` helper on the `_MoveTaskState` shell — the raw `mission_slug` local became the `st.mission_slug` field on the frozen state object, so the source token is now `st.mission_slug`; same `.exists()`-only probe, same disposition, only the line + receiver drifted; then 1325→732 (#2306 fold, mission tasks-py-degod-wave2-01KWH9EQ WP05 T021): the recorded 1325 was already one line stale on the mission base (actual 1326 — the off-by-one #2306 reports), and wave-2 WP02–WP04 relocations (tasks_shared.py, tasks_command_adapters.py, render-seam unification) removed ~594 lines above the sink; re-located via the gate's own AST audit; then tasks.py:732→tasks_move_task.py:204 by the same mission's WP05 move_task-family relocation (#2305) — `_mt_warn_worktree_kitty_specs` moved VERBATIM into `tasks_move_task.py`; same probe, same disposition, only the module + line changed; then 204→206 by degod-follow-ups PR #2308 constructor-DI collapse — a 2-line explanatory comment added to `_default_move_task_ports` above this sink) |
| cli/commands/agent/tasks_move_task.py:1634 | _mt_resolve_gate_baseline | baseline_read_dir / / wp_slug / | wp_slug | Path-join (/) | trusted-source | [inventory-only] Owned-checkout baseline read: `wp_slug = st.wp.path.stem`, derived from the canonically resolved on-disk WP file rather than external input; `baseline_read_dir = st.feature_dir` is the verified owned mission directory. The convergence flow analyzer correctly treats this derived segment as untainted, so the row remains as an explicit human-reviewed false-negative record. Read-only `BaselineTestResult.load()` semantics match the existing flagless row below. |
| cli/commands/agent/tasks_move_task.py:1303 | _mt_resolve_gate_baseline | return BaselineTestResult . load ( baseline_read_dir / / wp_… | wp_slug | Path-join (/) | trusted-source | `baseline_read_dir / "tasks" / wp_slug / "baseline-tests.json"`; `wp_slug` from `_resolve_wp_slug(...)` — the SAME derived-on-disk-filename provenance as `review/baseline.py:215`'s `capture_baseline` row (`wp.path.stem`-shaped, not external input). This is the shared baseline loader mission doctrine-controlled-transition-gates-01KY51Z7 (WP09) extracted from `_mt_run_pre_review_gate` so the doctrine-bound handler context and the FR-004 override tier diff against the SAME baseline artifact. Mission coord-commit-integrity-01KY5JS8 (C-008) then routed the parent dir through the SAME kind-aware read seam the review gate uses (`_resolve_workflow_read_dir(kind=WORK_PACKAGE_TASK)` → `resolve_feature_dir_for_mission` → `resolve_mission_read_path`, `assert_safe_path_segment`-guarded) instead of the coord-write `st.feature_dir` husk — so a coord-topology mission reads its PRIMARY-authored baseline, not the husk. Same read-only `BaselineTestResult.load()` semantics, same trusted `wp_slug` segment; disposition unchanged (base dir seam-resolved). |
| cli/commands/agent/workflow.py:2841 | review | _resolve_workflow_read_dir ( | wp_slug | Path-join (/) | trusted-source | `wp_slug = wp.path.stem`; parent dir now resolved by `_resolve_workflow_read_dir(...)` — the kind-aware read-side wrapper over `resolve_feature_dir_for_mission` → `resolve_mission_read_path` (assert_safe_path_segment). Segment derived, dir guarded. (line shifted −2 by mission 01KVJPEQ; then +7 by WP05 import addition +_analysis_report_gate_dir routing change; then 2647→2686 by mission implement-loop-coord-authority-completion-01KW2E7A (#2160) routing planning reads onto resolve_planning_read_dir; then 2686→2654 by the same mission's pre-merge-squad nit collapsing the vestigial _find_first_for_review_wp walk; then 2654→2670 by mission coord-read-residuals-01KW2M8V (#2186) WP01 identity routing adding primary folds above this sink; then 2670→2841 by mission read-surface-ssot-closeout-01KWZV91 WP04 re-routing the review write-candidate dir through the kind-aware read-side wrapper `_resolve_workflow_read_dir` (replacing the direct `resolve_feature_dir_for_mission` call; upstream/main still routes it directly) — same `wp_slug`-derived sink, disposition unchanged) |
| cli/commands/agent/workflow_cores.py:383 | _resolve_review_cycle_sub_artifact_dir | return feature_dir / / wp_slug | wp_slug | Path-join (/) | trusted-source | `wp_slug` is the WP file stem (`wp.path.stem`, threaded in via `claim_result.wp_slug`) — a derived on-disk filename, not external input. This is the raw-join FALLBACK branch of the new shared helper `_resolve_review_cycle_sub_artifact_dir` (2026-08-07, mission verdict-seam-write-unification-01KZ9Q35 WP05, T024): the helper's primary path routes through `review/cycle.py::_review_cycle_wp_dir` (the T058 seam owner); `feature_dir / "tasks" / wp_slug` is reached only when no workspace root is derivable (a bare test fixture with no git ancestor). CONSOLIDATES the two now-removed ghost rows below: `has_prior_rejection` (formerly `workflow_cores.py:372`) now calls this helper instead of joining inline; `implement_try_render_fix_mode_prompt` (formerly `workflow_executor.py:868`) was re-pointed to call `_review_cycle_wp_dir` DIRECTLY (bypassing this helper and its own former inline join entirely) — so that row is a straight ghost, not a relocation. Same trusted `wp_slug` provenance, disposition unchanged. The old `workflow_cores.py:372` (`has_prior_rejection`) and `workflow_executor.py:868` (`implement_try_render_fix_mode_prompt`) rows are removed as ghosts. |
| cli/commands/agent/workflow_executor.py:931 | implement_capture_baseline | baseline_artifact = feature_dir / / wp_slug / | wp_slug | Path-join (/) | trusted-source | Same `wp.path.stem`-derived `wp_slug` (via `claim_result.wp_slug`) provenance as `review/baseline.py:215`'s `capture_baseline` row; `feature_dir / "tasks" / wp_slug / "baseline-tests.json"` best-effort-commit candidate — the write counterpart of the baseline read. Pre-existed on the mission base as the underscore-prefixed local `_baseline_artifact = feature_dir / "tasks" / _wp_slug / "baseline-tests.json"` inline inside `workflow.py`'s pre-split monolithic `implement()`, but was never AST-discoverable because `_wp_slug` (leading underscore) sits outside `UNTRUSTED_SEGMENT_NAMES` — so it was never inventoried pre-split. Mission coord-authority-trio-degod-01KX7094 WP02's trio decomposition extracted the block into `implement_capture_baseline` with the parameter renamed `wp_slug` (underscore dropped), bringing the join into discovery scope for the first time. Same read-only-cache-write semantics as the pre-split code; disposition trusted-source. New row (not a relocation) — added by WP05 cycle-1 closeout. |
| cli/commands/agent/workflow_executor.py:1467 | _review_baseline_context_lines | rv_baseline_path = rv_feature_dir / / wp_slug / | wp_slug | Path-join (/) | trusted-source | Same `wp.path.stem`-derived `wp_slug` provenance, assigned directly at the call site (`wp_slug = wp.path.stem`, `workflow_executor.py:1549`); `rv_feature_dir / "tasks" / wp_slug / "baseline-tests.json"` read-only `BaselineTestResult.load(...)` for the review "BASELINE TEST CONTEXT" block — the read counterpart of `implement_capture_baseline`'s write. Pre-existed on the mission base as the underscore-prefixed local `_rv_baseline_path = _rv_feature_dir / "tasks" / _rv_wp_slug / "baseline-tests.json"` inline inside `workflow.py`'s pre-split monolithic review path, undiscoverable pre-split because `_rv_wp_slug` sits outside `UNTRUSTED_SEGMENT_NAMES`. Mission coord-authority-trio-degod-01KX7094 WP02's trio decomposition extracted the block into `_review_baseline_context_lines` with the parameter renamed `wp_slug`, bringing the join into discovery scope for the first time. Same read-only semantics; disposition trusted-source. New row (not a relocation) — added by WP05 cycle-1 closeout. |
| cli/commands/decision.py:552 | cmd_verify | mission_dir = _resolve_ledger_dir ( repo_root , mission_slug ) | mission_slug | Path-join (/) | routed-through-seam | [inventory-only] #5019 (mission coord-read-fail-closed, #4966): `cmd_verify`/`cmd_list`'s ledger read moved OFF `resolve_handle_to_read_path` onto `_resolve_ledger_dir` → `placement_seam(...).read_dir(PRIMARY_METADATA)` — the ledger's new PRIMARY home, so `decision list`/`verify` read the SAME dir the write path (`decisions/service.py`) resolves to (ending the coord-husk split-brain). The untrusted `mission_slug` is rejected at the CLI boundary by `_SAFE_SLUG_RE` (`^[A-Za-z0-9][A-Za-z0-9_-]*$`, the raw-token path-traversal reject in `_resolve_repo_root_and_slug`) BEFORE any resolution, and the seam it now calls is itself `assert_safe_path_segment`-guarded (via `_compose_primary_feature_dir` on the `PRIMARY_METADATA` leg). Matcher-invisible (cross-fn sink reached through the seam), so `[inventory-only]` and exempt from overcount — same status the prior `resolve_handle_to_read_path` row carried. |
| merge/done_bookkeeping.py:428 | _resolve_in_branch_status_events_path | rel_events_path = Path ( KITTY_SPECS_DIR ) / mission_slug / … | mission_slug | Path-join (/) | unreachable | Builds a *relative* `kitty-specs/<slug>/status.events.jsonl` only to stringify into a `git show <ref>:<path>` argument (`_assert_merged_wps_done_on_target` → `run_command(["git", "show", …])`); no local FS open — git resolves inside the tree object. (relocated VERBATIM from `cli/commands/merge.py:613` into the `_resolve_in_branch_status_events_path` seam by mission #2057 decompose-merge-god-module-01KVXHDK WP08 — behavior-preserving god-module decomposition, disposition unchanged; then 419→428 by mission coord-read-residuals-merge-lanes-and-identity-routing-01KW2M8V (#2185/#2186) merge/identity routing inserting lines above the helper (`def` moved 402→411) — same sink, only the line drifted, disposition unchanged) |
| merge/done_bookkeeping.py:430 | _resolve_in_branch_status_events_path | rel_events_path = Path ( KITTY_SPECS_DIR ) / mission_slug / … | mission_slug | Path-join (/) | unreachable | Same git-ref-argument path as :428 (the `if path_is_under_worktrees(...)` worktree-rewrite branch); no FS sink. (relocated VERBATIM from `cli/commands/merge.py:615` by mission #2057 WP08; then 421→430 by mission coord-read-residuals-merge-lanes-and-identity-routing-01KW2M8V (#2185/#2186) merge/identity routing inserting lines above the helper — same sink, only the line drifted, disposition unchanged) |
| merge/ordering.py:319 | _compute_next_mission_number_or_none | target_meta_path = scan_specs / mission_slug / | mission_slug | Path-join (/) | routed-through-seam | #2037 closure: `mission_slug` is now checked with `assert_safe_path_segment` before this join; an unsafe slug logs a warning and skips the idempotency peek entirely (fail-closed by falling through to normal `assign_next_mission_number`, matching this function's existing "best-effort" contract). (line shifted 297→319 by this fix — the join now lives inside the `else` branch of the new guard; same sink, disposition now closed. Prior history: relocated VERBATIM from `cli/commands/merge.py:1144` into the mission-number bake cluster by mission #2057 decompose-merge-god-module-01KVXHDK WP07 — behavior-preserving god-module decomposition) |
| merge/ordering.py:327 | _compute_next_mission_number_or_none | target_meta = load_meta ( scan_specs / mission_slug , on_mal… | mission_slug | Path-join (/) | routed-through-seam | #2037 closure: sibling of the :319 sink — same `assert_safe_path_segment` guard above both joins in the same function body covers this second `scan_specs / mission_slug` join feeding `load_meta`. (line shifted 304→327 by the same fix. Prior history: NEW row surfaced by mission read-surface-ssot-closeout-01KWZV91 replacing the prior `_json.loads(target_meta_path.read_text())` read with the canonical `load_meta` reader) |
| coordination/surface_resolver.py:552 | _coord_mid8 | coord_candidate = repo_root / / / KITTY_SPECS_DIR / mission_… | mission_slug | Path-join (/) | unreachable | Path composed solely to populate `StatusReadPathNotFound(coord_candidate=…)` inside a `raise`; never opened/written — fail-closed diagnostic payload. (line shifted by mission 01KVGCE8 collapse; then 518→472 by mission 01KVN754 WP04 coord-empty Option B deletion of CoordinationWorktreeEmpty + 2 helpers; then 472→487 by 01KVPR00 WP03; then 487→489 by 01KVPR00 WP08/WP09 surface-topology gating; then 489→494 by mission 01KVRJ6P WP02 FR-005 collapse of `_COORD_SURFACE_TOPOLOGIES` + import-block widen; then 494→493 by 3.2.3-coord-surface-regressions #2119/#2125 retrospective-home docstring/error-prose edit in `_coord_mid8`; then 493→494 by mission single-authority-resolution-gates-01KW1P0F WP04 routing `resolve_status_surface_with_anchor` through `_canonicalize_primary_read_handle` (import + 3-line call split above this join); then 494→495 by PR #2277 reliability-papercut-sweep CoordinationBranchDeleted error-message reword (+1 line above this join); then 495→499 by mission coord-primary-partition-lock-01KWZ46V (squash-merged 007528ddf) reworking the `coord_candidate` composition from `CoordinationWorkspace.worktree_path(repo_root, mission_slug, "")` to a direct `repo_root / ".worktrees" / f"{mission_slug}-coord" / KITTY_SPECS_DIR / mission_slug` join (#2091, invariant M-1: `CoordinationWorkspace.worktree_path` now REQUIRES a non-empty mid8 and would raise `CoordinationWorkspaceIdentityUnresolved` before this more specific fail-closed `StatusReadPathNotFound` could raise) — still the same diagnostic-only, no-FS-sink `raise` payload, disposition unchanged; then 499→552 by PR #845 (#801 fix rounds: merged-primary pre-check + event-log fall-through helpers inserted above `_coord_mid8`; the join itself kept its pre-PR multiline form)) |
| coordination/surface_resolver.py:504 | _coord_mid8 | primary_candidate = repo_root / KITTY_SPECS_DIR / mission_sl… | mission_slug | Path-join (/) | unreachable | Same fail-closed `raise` payload (`primary_candidate=…`); diagnostic Path, no FS sink. (line shifted by mission 01KVGCE8 collapse; then 523→477 by mission 01KVN754 WP04; then 477→492 by 01KVPR00 WP03; then 492→494 by 01KVPR00 WP08/WP09; then 494→499 by mission 01KVRJ6P WP02 FR-005 frozenset collapse; then 499→498 by 3.2.3-coord-surface-regressions #2119/#2125 retrospective-home docstring/error-prose edit in `_coord_mid8`; then 498→499 by mission single-authority-resolution-gates-01KW1P0F WP04 routing `resolve_status_surface_with_anchor` through `_canonicalize_primary_read_handle`; then 499→500 by PR #2277 reliability-papercut-sweep CoordinationBranchDeleted error-message reword (+1 line above this join); then 500→504 by mission coord-primary-partition-lock-01KWZ46V (squash-merged 007528ddf) expanding the sibling `coord_candidate` join above from a single-call form into a 5-line composed `Path` expression (#2091 rework, same commit) — same `primary_candidate` join, only the line drifted, disposition unchanged) |
| migration/verdict_provenance_backfill.py:170 | _review_cycle_candidate_dirs | exact = tasks_dir / wp_id | wp_id | Path-join (/) | unreachable | `wp_id` is a Named-untrusted source (`NAMED_UNTRUSTED`, SC-003) so this row cannot be dispositioned `trusted-source` regardless of provenance — but `tasks_dir / wp_id` is consumed only by `.is_dir()` here (an existence probe feeding a read-only fan-out list of candidate review-cycle directories); the caller only `.glob()`s `review-cycle-*.md` files under the returned dirs — no open/write reachable via the raw join. Byte-identical shape and disposition to the sibling `cli/commands/_review_cycle_reconcile_doctor.py:133` row (also `exact = tasks_dir / wp_id`, also `unreachable`). Provenance is in fact on-disk-derived (`discover_wp_ids_with_review_cycles` matches `tasks/` subdirectory names against a `WP\d+` prefix regex, never external/CLI input), same as that sibling row's own `wp_id`. New row (2026-08-07, mission verdict-seam-write-unification-01KZ9Q35 WP02, FR-012 backfill migration). |
| migration/backfill_runtime_state.py:581 | backfill_runtime_state_repo | candidates = [ kitty_specs / mission_slug ] if ( kitty_specs… | mission_slug | Path-join (/) | routed-through-seam | `backfill_runtime_state_repo`: `assert_safe_path_segment(mission_slug)` rejects lexical traversal before either `kitty-specs/<slug>` join, then `ensure_within_any(..., roots=[kitty_specs])` validates the resolved selected path and every enumerated directory. A selected symlink escape fails closed; a full-corpus walk logs and skips it before downstream writes. Hostile selector and selected/full-walk symlink regressions in `tests/unit/migration/test_backfill_runtime_state.py` pin both seams. |
| migration/runtime_state_cutover.py:209 | cutover_repo | candidate = kitty_specs / mission_slug | mission_slug | Path-join (/) | routed-through-seam | `cutover_repo`: `assert_safe_path_segment(mission_slug)` rejects lexical traversal before the `kitty-specs/<slug>` join, then `ensure_within_any(..., roots=[kitty_specs])` validates the resolved selected path and every enumerated directory. A selected symlink escape fails closed; a full-corpus walk logs and skips it before event-log/snapshot/meta writes. Hostile selector and selected/full-walk symlink regressions in `tests/specify_cli/migration/test_runtime_state_cutover.py` pin both seams. |
| migration/mission_state.py:658 | _compare_checkout_mission_state | invoking_sha = _artifact_sha256 ( invoking_specs / slug / ar… | slug | Path-join (/) | trusted-source | `slug` is a directory name discovered by `iterdir()` over `kitty-specs/` (existing dirs only, `p.is_dir()`), optionally narrowed to a single `--mission` handle via set-intersection — never a raw external path. Read-only: `_artifact_sha256(kitty-specs/<slug>/{status.json,status.events.jsonl})` content-hashes the artifact to detect cross-checkout disagreement (no open-for-write). Same derived-on-disk provenance as the `mission_state.py` `run_id` row; disposition trusted-source. New row (mission worktree-root-resolution-01M0B59R, WP04 `_compare_checkout_mission_state`; PR #3567). |
| migration/mission_state.py:659 | _compare_checkout_mission_state | primary_sha = _artifact_sha256 ( primary_specs / slug / arti… | slug | Path-join (/) | trusted-source | `slug` is a directory name discovered by `iterdir()` over `kitty-specs/` (existing dirs only, `p.is_dir()`), optionally narrowed to a single `--mission` handle via set-intersection — never a raw external path. Read-only: `_artifact_sha256(kitty-specs/<slug>/{status.json,status.events.jsonl})` content-hashes the artifact to detect cross-checkout disagreement (no open-for-write). Same derived-on-disk provenance as the `mission_state.py` `run_id` row; disposition trusted-source. New row (mission worktree-root-resolution-01M0B59R, WP04 `_compare_checkout_mission_state`; PR #3567). |
| migration/mission_state.py:1737 | _repair_mission | quarantine_path = repo_root / MISSION_STATE_AUDIT_ROOT / / r… | run_id | Path-join (/) | trusted-source | `run_id = _compute_run_id(...)` is a SHA-256 hex digest (16 chars) derived deterministically from repo-local file content hashes — it is never a CLI argument or external input. The segment is safe by construction (the sibling `<slug>` segment is `assert_safe_path_segment`-guarded before the join). (token re-pinned by PR #5008 / #4928 — the quarantine root moved from the gitignored `MANIFEST_ROOT` (`.kittify/migrations/mission-state`) to the git-TRACKED `MISSION_STATE_AUDIT_ROOT` (`.kittify/mission-state-audit`) so the audit trail survives `git clean`; same sink, same trusted `run_id` segment. Prior identity: line 1520, token `repo_root / MANIFEST_ROOT`, re-pinned by PR #4800; prior-prior line 1054, token `repo_root`, shifted 1053→1054 by mission write-surface-coherence-01KVTVZS.) |
| migration/mission_state.py:758 | repair_repo | _resolve_repo_relative ( resolved_repo_root , MISSION_STATE_… | run_id | Path-join (/) | trusted-source | Read-only path computation: `repair_repo` derives the quarantine-root display path (`_resolve_repo_relative(resolved_repo_root, MISSION_STATE_AUDIT_ROOT / "quarantine" / run_id)`) only to record `quarantine_root_path` on the `RepairReport` — no open-for-write happens here. `run_id = _compute_run_id(...)` is a deterministic SHA-256 hex digest of repo-local file content, never a CLI argument or external input, so the segment is safe by construction. New row (PR #5008 / #4928 — `RepairReport.quarantine_root_path` exposure alongside the audit-trail relocation to the git-TRACKED root). |
| missions/_read_path_resolver.py:1303 | _compose_primary_feature_dir | primary_dir : Path = get_main_repo_root ( repo_root ) / KITT… | mission_slug | Path-join (/) | routed-through-seam | `_compose_primary_feature_dir` calls `assert_safe_path_segment(mission_slug)` immediately before `get_main_repo_root(repo_root) / KITTY_SPECS_DIR / mission_slug` — byte-identical join to the row this replaces. Seam present. RENAMED from `primary_feature_dir_for_mission` (formerly inventoried at `missions/_read_path_resolver.py:1239`) by mission read-side-seam-primary-primitive-closure-01KYKMMT WP08 (T035/SC-001), which deleted the public wrapper and extracted this same join, verbatim, into the module-private `_compose_primary_feature_dir` leaf (WP03/T015) that the wrapper's remaining callers were re-pointed at in the same commit — same sink, same untrusted `mission_slug` provenance, same seam guard, disposition unchanged. The old `missions/_read_path_resolver.py:1239` row (and its full line-shift history) is removed as a ghost; see `tests/architectural/surface_resolution_audit/inventory.md` for the sibling WP01-census row covering the same leaf. |
| review/baseline.py:215 | capture_baseline | artifact_dir = feature_dir / / wp_slug | wp_slug | Path-join (/) | trusted-source | `feature_dir / "tasks" / wp_slug`; `wp_slug` is the WP task-file slug (derived on-disk filename), `feature_dir` supplied trusted. Read/cache of `baseline-tests.json`. |
| review/cycle.py:52 | _review_cycle_wp_dir | return mission_dir / / wp_slug | wp_slug | Path-join (/) | routed-through-seam | Canonical `review-cycle://` parse: `validate_review_cycle_pointer` validates `mission_slug`/`wp_slug`/`filename` (each `assert_safe_path_segment`-guarded via `_validate_segment`) before the join. RELOCATED from `resolve_review_cycle_pointer` (formerly inventoried at `review/cycle.py:193`) by mission coord-commit-integrity-01KY5JS8: the shared read/write helper `_review_cycle_wp_dir` now resolves the mission dir through the kind-aware `resolve_planning_read_dir(kind=WORK_PACKAGE_TASK)` seam (`review-cycle-N.md` is a PRIMARY-partition WORK_PACKAGE_TASK artifact), retiring the kind-blind `candidate_feature_dir_for_mission` fold that mislanded on the coord husk for coord-topology missions (#2646/#2697/#2275). `MissionSelectorAmbiguous` propagates (no silent pick — C-009). Segment validated + base seam-resolved; disposition unchanged. The old `review/cycle.py:193` row is removed as a ghost. |
| review/cycle.py:148 | _review_cycle_wp_dir | return resolved_dir / / wp_slug | wp_slug | Path-join (/) | routed-through-seam | WP13 (T058): `_review_cycle_wp_dir` was parametrized with an explicit `kind` (default `WORK_PACKAGE_TASK`); this is the generic `else` branch's join (`resolved_dir = seam.read_dir(kind)` then `/ "tasks" / wp_slug`), a sibling of the `REVIEW_CYCLE`-branch join at `:145` (this file's own `:52` row, line-drifted, same token). Same caller-facing contract as that row: every production call site threads `wp_slug` from `_resolve_wp_slug` (`tasks_materialization.py`), itself scanning an on-disk `tasks/` listing, not external input; `mission_slug`/`wp_slug` are `assert_safe_path_segment`-guarded upstream at every writer/pointer call site (`_validate_segment`). Disposition unchanged. |
| review/cycle.py:231 | resolve_review_cycle_pointer | candidate = ( common_dir / / / mission_slug / wp_slug / file… | wp_slug | Path-join (/) | routed-through-seam | Legacy `feedback://` parse: `mission_slug`/`wp_slug`/`filename` each pass `_validate_segment` → `assert_safe_path_segment` before the join. Seam present. (line shifted 185→231 by mission retrospective-durable-home-01KVYM1W #2136/#2164 cure adding the canonical review-cycle:// fold above this legacy branch.) |
| status/aggregate.py:514 | MissionStatus._find_meta_path | raw_meta = primary_dir / _META_JSON_FILENAME | mission_slug | Path-join (/) | routed-through-seam | [inventory-only] `_find_meta_path`: reached only from `MissionStatus.load`, which calls `_validate_mission_slug` → `assert_safe_path_segment` (raises `InvalidMissionSlug`) at line 228 before any path composition. Seam present. |
| status/aggregate.py:762 | MissionStatus.save | meta_path = diag_primary / _META_JSON_FILENAME , | mission_slug | Path-join (/) | routed-through-seam | [inventory-only] `self.mission_slug` on a `MissionStatus` instance constructed via `load` (slug already validated at construction, line 228); path composed only for a `MissionMetadataUnavailable` diagnostic. |
| status/aggregate.py:763 | MissionStatus.save | primary_candidate = diag_primary , | mission_slug | Path-join (/) | routed-through-seam | [inventory-only] Same validated-at-construction `self.mission_slug`; `primary_candidate` diagnostic Path. |
| status/lifecycle.py:427 | generate_lifecycle_json | output_dir = derived_dir / mission_slug | mission_slug | Path-join (/) | routed-through-seam | `generate_lifecycle_json`: `mission_slug = lifecycle.mission_slug or feature_dir.name`; `lifecycle.mission_slug` derives from the snapshot sanitised by `reducer.safe_mission_slug` (reducer.py:162). Seam present. |
| status/progress.py:219 | generate_progress_json | output_dir = derived_dir / mission_slug | mission_slug | Path-join (/) | routed-through-seam | `generate_progress_json`: `mission_slug = snapshot.mission_slug or feature_dir.name`; `snapshot.mission_slug` sanitised by `reducer.safe_mission_slug`. Seam present. |
| status/store.py:222 | _SlugResolver.resolve | meta_path = self . _mission_specs_root / mission_slug / | mission_slug | Path-join (/) | routed-through-seam | `MissionIdResolver.resolve`: at line 213 `_is_safe_slug(mission_slug)` → `assert_safe_path_segment` validates the slug fail-closed before this join. Additionally, `_is_contained` (called at line 223) validates the *resolved* path via `ensure_within_any`, catching symlink escapes (FR-002). Both grammar and containment seams present. |
| status/views.py:92 | write_derived_views | output_dir = derived_dir / mission_slug | mission_slug | Path-join (/) | routed-through-seam | `write_derived_views`: `mission_slug = snapshot.mission_slug or feature_dir.name`; `snapshot.mission_slug` sanitised by `reducer.safe_mission_slug` before the `derived_dir / mission_slug` `.mkdir()` + writes. Seam present. |
| status/views.py:266 | materialize_if_stale | feature_derived = derived_dir / mission_slug | mission_slug | Path-join (/) | routed-through-seam (TODO) | **RESOLVED at source by WP02 (FR-009).** `materialize_and_refresh_views`: `mission_slug = _stale_check_slug(feature_dir)` → `resolve_mission_identity`, which now routes the `meta.json` slug through `safe_mission_slug(..., feature_dir.name)` (fail-closed). The matcher still flags this join because the seam is **upstream** (cross-function flow is a documented RULESET known-FN), not at the join site. Closed — verified by the WP02 mutation review (reverting the chokepoint made the hostile-meta test create an escaped dir). TODO tag retained only as the matcher-per-site-seam artifact. |
| mission_metadata.py:458 | write_meta | atomic_write ( meta_path , content ) | meta["mission_slug"] (via feature_dir) | atomic_write(...) | routed-through-seam (TODO) | [inventory-only] **RESOLVED by WP02 (FR-009).** `resolve_mission_identity` now sanitises the `meta.json` slug via `safe_mission_slug` before it can key any downstream path; `write_meta`'s `feature_dir / "meta.json"` is keyed off the (now-trusted) slug. Inventory-only assertion (RULESET §6); the matcher cannot trace the source-level seam across functions, so the row is retained as a known-FN artifact, not an open hole. |
| cli/commands/cutover_guard.py:135 | evaluate_touched_missions | mission_dir = corpus / slug | slug (diff-derived, from `touched_mission_slugs` `parts[1]`) | Path-join (/) then `.is_dir()` + event-log reads | routed-through-seam | Genuinely untrusted: the slug is lifted verbatim out of a PR diff path. `evaluate_touched_missions` now calls `assert_safe_path_segment(slug)` before the join and records a fail-CLOSED `CutOverVerdict` on rejection, consistent with every other uncertainty in the loop (NFR-003) — a rejected slug can never be silently skipped into a passing guard. |
| cli/commands/_review_cycle_reconcile_doctor.py:133 | _shape_artifact_dirs_for_wp | exact = tasks_dir / wp_id | wp_id | Path-join (/) | unreachable | WP08 (FR-008) reconciliation-detector REPLICA of the FORMER `post_merge/review_artifact_consistency.py:59` `_artifact_dirs_for_wp` join — deliberately does not call the live function, precisely so it keeps working after WP13's T059 rewrite. That `:59` row (`exact = tasks_dir / wp_id`, disposition `unreachable`) is REMOVED as a ghost here: WP13 rewrote `_artifact_dirs_for_wp` to resolve a single directory through `review/cycle.py::_review_cycle_wp_dir` (T058's owner function) instead of this module's own raw `tasks_dir / wp_id` fan-out join, so the exact token no longer exists in that file. `wp_id` comes from this module's own on-disk enumeration (`_wp_ids_under_tasks`'s `_WP_ID_RE` match over `tasks/` subdirectory names it just listed), never external input; `tasks_dir / wp_id` is consumed only by `.is_dir()` (this line) and, in the caller, `.glob()` reads — no open/write of the raw join, same disposition as its (now-retired) live twin. |
| cli/commands/_review_cycle_reconcile_doctor.py:158 | _shape_review_cycle_wp_dir | candidate = primary_feature_dir / / wp_slug | wp_slug | Path-join (/) | unreachable | WP08 (FR-008) reconciliation-detector REPLICA of `review/cycle.py:52`'s `_review_cycle_wp_dir` (see that row above) — deliberately does not call the live function. `wp_slug` comes from this module's own on-disk enumeration (`_wp_ids_under_tasks`), never external input; the composed `candidate` is consumed only by `.is_dir()` here and `.glob()` reads in the caller — no open/write. |
| cli/commands/_review_cycle_reconcile_doctor.py:174 | _shape_arbiter_bare_wp_id_dir | candidate = primary_feature_dir / / wp_id | wp_id | Path-join (/) | unreachable | WP08 (FR-008) reconciliation-detector REPLICA of the bare-`wp_id` join `review/arbiter.py::_find_review_cycle_artifact` (formerly `:388`) and `::get_arbiter_overrides_for_wp` (formerly `:523`) used to share — deliberately does not call the live functions, precisely so it keeps working after they are rewritten or deleted (as they now have been). `_find_review_cycle_artifact`'s row and `get_arbiter_overrides_for_wp`'s row are REMOVED as ghosts here: mission `review-cycle-verdict-seam-rebuild-01KZ2W7W` WP12 (FR-009, arbiter-override-retirement) retired both — the former deleted outright, the latter reimplemented over the event-sourced `ReviewOverride` snapshot slot (`specify_cli.status.wp_snapshot_state`) with no `tasks_dir / wp_id` join left in its body. `wp_id` here comes from this module's own on-disk enumeration (`_wp_ids_under_tasks`'s `_WP_ID_RE` match over `tasks/` subdirectory names it just listed), never external input; the composed `candidate` is consumed only by `.is_dir()` here and `.glob()` reads in the caller — no open/write. |

## Disposition summary

| disposition | count | meaning |
| --- | --- | --- |
| routed-through-seam | 19 | already safe (seam cited); +4 (#2037 closure) — `cli/commands/agent/tasks_move_task.py:302`, `cli/commands/decision.py:552`, `merge/ordering.py:319`, `merge/ordering.py:327` each route through `assert_safe_path_segment`. `decision.py:552` was re-pointed by #5019 (mission coord-read-fail-closed): its ledger read moved off `resolve_handle_to_read_path` onto `_resolve_ledger_dir` → `placement_seam(...).read_dir(PRIMARY_METADATA)`, so the in-file guard is now the `_SAFE_SLUG_RE` raw-token boundary reject plus the seam's own `assert_safe_path_segment` — see the row above. |
| routed-through-seam (TODO) | 2 | 2 RESOLVED-at-source by WP02 (FR-009; matcher-per-site artifact). |
| trusted-source | 10 | derived directory-name / on-disk index / SHA-256 hex provenance |
| unreachable | 9 | no FS open/write reachable with an untrusted segment (+3, WP08 FR-008 reconciliation-detector shape replicas); -2 (`review/arbiter.py:388`/`:523`) removed as ghosts by mission review-cycle-verdict-seam-rebuild-01KZ2W7W WP12 (FR-009); -1 (`post_merge/review_artifact_consistency.py:59`) removed as a ghost by the SAME mission's WP13 (T059 rewrite of `_artifact_dirs_for_wp`); +1 (`migration/verdict_provenance_backfill.py:170`) added by the 2026-08-07 landing-pass reconciliation above (`wp_id` is Named-untrusted, SC-003, so this `.is_dir()`-only sink cannot be `trusted-source`) |
| **total** | **40** | dispositioned rows in the sink table |

Of the 40 rows, 35 are AST-discovered (the live matcher prints
the exact discovered count each run) and 5 are `[inventory-only]`
known-false-negative rows (the FR-009 `mission_metadata.py` write-path + four
cross-function diagnostics the matcher cannot trace across a boundary seam).
`audit.py` asserts both directions: every AST-discovered row appears here
(undercount) AND every non-tagged row still maps to a live sink (overcount).

**WP05 cycle-1 closeout reconciliation (mission coord-authority-trio-degod-01KX7094).**
WP02's trio decomposition split `cli/commands/agent/workflow.py`'s monolithic
`implement()`/review path into `workflow_cores.py` + `workflow_executor.py`
without reconciling this inventory, producing 2 ghost rows + 4 undocumented
sinks (all `wp_slug`/`wp.path.stem`-derived, trusted-source, same provenance
class as the pre-existing `workflow.py`/`review/baseline.py` rows):
- **Ghosts removed** (superseded by the relocation rows below): `workflow.py:921`
  (`_has_prior_rejection`) and `workflow.py:1621` (`implement`, `_sub_artifact_dir`).
- **Added** — 2 verbatim relocations with a qualname/underscore rename:
  `workflow_cores.py:372` (`has_prior_rejection`) and `workflow_executor.py:868`
  (`implement_try_render_fix_mode_prompt`).
- **Added** — 2 newly-discoverable sinks that pre-existed on the mission base as
  underscore-prefixed locals (`_baseline_artifact` / `_rv_baseline_path`,
  outside `UNTRUSTED_SEGMENT_NAMES`) and only entered discovery scope once the
  trio split's parameter renaming dropped the leading underscore:
  `workflow_executor.py:931` (`implement_capture_baseline`) and
  `workflow_executor.py:1467` (`_review_baseline_context_lines`).

**2026-08-07 landing-pass reconciliation (mission verdict-seam-write-unification-01KZ9Q35,
PR #3245).** WP05's T024 extraction and WP02's FR-012 backfill migration each
added/moved a raw-join site without reconciling this inventory, producing 2
ghost rows + 2 undocumented sinks:
- **Ghosts removed** (consolidated into the row below): `workflow_cores.py:372`
  (`has_prior_rejection`) and `workflow_executor.py:868`
  (`implement_try_render_fix_mode_prompt`) — WP05 extracted both call sites'
  formerly-independent inline joins into one shared helper,
  `_resolve_review_cycle_sub_artifact_dir`, and separately re-pointed
  `implement_try_render_fix_mode_prompt` to call `_review_cycle_wp_dir`
  directly, leaving neither former qualname with a raw join of its own.
- **Added** — the consolidated helper's fallback-branch join:
  `workflow_cores.py:383` (`_resolve_review_cycle_sub_artifact_dir`).
- **Added** — a genuinely new site: `migration/verdict_provenance_backfill.py:170`
  (`_review_cycle_candidate_dirs`), WP02's FR-012 backfill migration scanning
  `tasks/<wp_id>` candidate directories with an on-disk-derived `wp_id`.

**WP02/WP03 fixes applied:** The following joins were removed from the discovery
set by routing through the canonical seam (no longer discovered by `audit.py`):
- `dossier/drift_detector.py` — `assert_safe_path_segment` before `dossiers/<slug>` mkdir/open
- `dossier/snapshot.py` — `assert_safe_path_segment` before `dossiers/<slug>` mkdir/open
- `events/decision_log.py` — `assert_safe_path_segment` before `KITTY_SPECS_DIR/<slug>` open
- `migration/mission_state.py` — `assert_safe_path_segment` before quarantine `<slug>` join (run_id remains trusted-source)
- `review/arbiter.py` — `assert_safe_path_segment` before `tasks/<wp_id>` mkdir + `write_text_within_directory`

## Routing of the remaining `routed-through-seam (TODO)` rows

- **WP02 (status/) — DONE:** `status/views.py:266` and the FR-009
  `mission_metadata.py:458` `meta.json` write-path bypass are **closed at source**
  by WP02 (`resolve_mission_identity` → `safe_mission_slug`, fail-closed). They
  remain tagged `(TODO)` only because the matcher inspects the join site and the
  seam is upstream (cross-function flow — a documented RULESET known-FN), not
  because the vulnerability is open. Verified by the WP02 mutation review.
- **#2037 (parent #1868) — DONE:** the 4 CLI-arg (`--mission`), read-only /
  existence-probe sinks still live in this tree are now closed.
  `cli/commands/agent/tasks_move_task.py:302` and
  `merge/ordering.py:319`/`:327` each gained an `assert_safe_path_segment`
  guard immediately before their join, failing closed by skipping the
  candidate/probe/peek on an unsafe slug rather than raising (each site is a
  read-only or best-effort operation, so dropping one candidate must not abort
  the caller). `cli/commands/decision.py` was, at the #2037 closure, routed
  through `resolve_handle_to_read_path`; #5019 later re-pointed its ledger read
  onto `_resolve_ledger_dir` → `placement_seam(...).read_dir(PRIMARY_METADATA)`
  (the ledger's new PRIMARY home), leaving the `_SAFE_SLUG_RE` raw-token
  boundary reject plus the seam's own `assert_safe_path_segment` as the guards —
  see `decision.py:552` in the sink table.
- **Runtime-state library selectors — DONE:**
  `migration/backfill_runtime_state.py:577` and
  `migration/runtime_state_cutover.py:206` both validate the optional selector
  with a local `assert_safe_path_segment` guard before joining or writing.

## T005 — `status/aggregate.py` raise-guard + composed reads (FR-003)

`MissionStatus._validate_mission_slug` (aggregate.py:345) delegates to
`assert_safe_path_segment` and **raises `InvalidMissionSlug`** on any unsafe
slug. It is called at the top of `MissionStatus.load` (line 228) *before* any
path composition, so every downstream composed read — `_find_meta_path`
(`primary_dir / "meta.json"`, line 431; the `specs_dir.glob(f"{mission_slug}-*/
meta.json")` first-match glob, line 473) and the `_read_meta` `meta_path.
read_text` (line 375) — runs on an already-validated slug. **Disposition:**
`routed-through-seam` (the load-boundary raise-guard is the seam). The
historical silent-first-match glob (line 473) is a *selection* ambiguity
(S8 follow-up), **not** a traversal hole — the slug is already grammar-checked,
so the glob cannot escape the specs root.

## Anti-overfit demonstration (T004)

The seed-set is data. Temporarily adding `"filename"` to
`UNTRUSTED_SEGMENT_NAMES` and re-discovering surfaces **+35 new joins** across
unrelated modules absent from this table — e.g.
`cli/commands/charter/_status_collectors.py:72`, `cli/commands/doctor.py:281`,
`dashboard/handlers/features.py:408`, `doctrine/sources/api_source.py:179` —
proving the matcher generalises rather than hard-coding the known list. The
symbol is **not** committed to the seed-set (`filename` is not a mission-domain
untrusted segment in this audit's scope); the experiment is reproducible by
adding it to `audit.py`'s `UNTRUSTED_SEGMENT_NAMES` and re-discovering.

## Audited-surface list (WP04 anchor)

The stable surface list WP04's guard anchors on is maintained as a separate
machine-readable artifact: `audited-surfaces.md`.
