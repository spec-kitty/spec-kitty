# FUSE temporary files (created by filesystem-in-userspace on some Linux systems)
.fuse_hidden*

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg


# Virtual environments
venv/
ENV/
env/
.venv
.venv*/
.uv-tools/
.pytest_cache

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Project specific
*.log
# Investigation mission evidence logs are first-class review artifacts.
!kitty-specs/**/research/*.log
.env
.env.local
.codex/
.claudeignore
# Personal pyproject overrides for cross-package dev (Pattern B in
# docs/development/local-overrides.md). Never committed.
pyproject.local.toml
*.lock
# uv.lock is the canonical lockfile for spec-kitty-cli (NFR-005 of mission
# shared-package-boundary-cutover-01KQ22DS); track it explicitly.
!uv.lock
.worktrees/
.kittify/.dashboard
.kittify/active-mission
.kittify/missions/
.kittify/scripts/
.kittify/runtime/
.kittify/derived/
.kittify/events/
.kittify/dossiers/
.kittify/workspaces/
.kittify/config.toml
.kittify/merge-state.json
.kittify/sync-state.json
.kittify/lint-report.json
.kittify/preflight-warning.json
.kittify/.migration-backup/
.kittify/encoding-provenance/
.test-full-status
.kittify/migrations/
.kittify/logs/
# Per-machine skill install ledger (timestamps, hashes, delivery_mode) — #2412.
.kittify/skills-manifest.json

# Local performance cache; durable Op records live in kitty-ops/<op-id>.jsonl.
kitty-ops/ops-index.jsonl
kitty-specs/**/.kittify/config.yaml
kitty-specs/**/.kittify/dossiers/

# Dossier snapshots are mutable derived artifacts; recomputable from dossier source.
# See kitty-specs/charter-e2e-827-followups-01KQAJA0/contracts/dossier-snapshot-ownership.md
.kittify/dossiers/*/snapshot-latest.json
kitty-specs/*/.kittify/dossiers/*/snapshot-latest.json
.kittify/charter/context-state.json

# Doctrine artifacts are version-controlled — that is the point of
# `spec-kitty agent retrospect synthesize --apply` writing them. Use
# `/**` (not the bare directory) so the doctrine directory itself stays
# traversable for whitelists; then re-include the canonical artifact
# subdirectories (directive/, tactic/, procedure/, overlays/). Tracked
# upstream as Priivacy-ai/spec-kitty#1166.
.kittify/doctrine/**
!.kittify/doctrine/directive/
!.kittify/doctrine/directive/**
!.kittify/doctrine/tactic/
!.kittify/doctrine/tactic/**
!.kittify/doctrine/styleguide/
!.kittify/doctrine/styleguide/**
!.kittify/doctrine/procedure/
!.kittify/doctrine/procedure/**
!.kittify/doctrine/overlays/
!.kittify/doctrine/overlays/**
!.kittify/doctrine/graph.yaml

# Generated glossary entity pages — never commit
.kittify/charter/compiled/glossary/

# Local-only working notes / mission drafts (already-tracked files remain tracked)
/work/
.kittify/.scratch
node_modules/

# Spec Kit-specific files
.genreleases/
*.zip
sdd-*/

# Added by Spec Kitty CLI (auto-managed)
.claude/
.agents/
.agents
.opencode/
.windsurf/
.gemini/
.cursor/
.qwen/
.kilocode/
.augment/
.github/*
!.github/workflows/
!.github/workflows/*.yml
!.github/PULL_REQUEST_TEMPLATE.md
!.github/ISSUE_TEMPLATE/
!.github/ISSUE_TEMPLATE/*
# Shared composite actions (mission ci-pipeline-reinstatement-01M1X35E, WP04/FR-003)
!.github/actions/
!.github/actions/**
# Committed module-shard registry + shard-timings (mission ci-pipeline-reinstatement-01M1X35E, WP08/FR-006)
!.github/ci-module-registry.yml
!.github/ci-shard-timings.json
# Committed MEASURED coverage-honesty baseline (mission ci-coverage-honesty-01M3CZVN, WP01/FR-001+FR-002)
!.github/ci-foreign-coverage-baseline.json
.roo/
.amazonq/
.pytest_cache/
# Note: kitty-specs/ is tracked in main repo but ignored in worktrees via .git/info/exclude
# Test-fixture missions generated by CLI/E2E test runs (never commit)
kitty-specs/test-feature-*
kitty-specs/*-123-test-feature
.github/copilot/

# User-managed glossaries (optional)
.kittify/glossaries/team_domain.yaml
.kittify/glossaries/audience_domain.yaml
.kittify/glossaries/mission_local.yaml

# Keep spec_kitty_core.yaml tracked (canonical terms)
!.kittify/glossaries/spec_kitty_core.yaml
doctrine_ref
/work/

# Mutation testing
mutmut.db
mutmut-cache/
.mutmut-cache
# anchored to repo root: mutmut writes its artifact dir at the root. Unanchored,
# this swallowed scripts/mutants/ (this repo's tracked mutation-testing plugins)
# at any depth, silently dropping any new plugin from `git add` (#3125).
/mutants/

# CI report outputs
.coverage
out/reports/**
!out/reports/
!out/reports/.gitkeep
!out/reports/xunit-reports/
!out/reports/xunit-reports/.gitkeep
!out/reports/coverage/
!out/reports/coverage/.gitkeep
!out/reports/static-analysis/
!out/reports/static-analysis/.gitkeep

src/doctrine/curation/imports/**
!src/doctrine/curation/imports/.gitkeep
!src/doctrine/curation/imports/**/
!src/doctrine/curation/imports/**/*.yaml
!src/doctrine/curation/imports/**/*.yml

work
out
.nvimlog

tmp/**
!tmp/.gitkeep
pytest-of-*/
.kittify/missions/__pycache__/
.spec-kitty/
# Clone-local runtime-state root for isolated dev environments (Shadow Clones).
# Populated when SPEC_KITTY_HOME points here — see
# docs/development/isolated_dev_environments.md. Never commit machine state.
.spec-kitty-home/
.agent/

# CycloneDX SBOM + audit artifacts are generated by the release workflow.
*.cdx.json
.kittify/ticket-context.md
.kittify/pending-origin.yaml
.kittify/mission-brief.md
.kittify/brief-source.yaml
.kittify/legacy-warning-shown-*
.local/

# Build-time PlantUML jar (downloaded + sha256-verified in CI/tests, never committed)
/plantuml.jar

# Spec Kitty per-repo operator env file (local operator config)
.kittify/.kitty.env
